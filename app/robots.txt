import { MetadataRoute } from 'next';

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: '*',
      allow: '/',
      disallow: '/admin/', // Empêche l'indexation de tes pages sensibles
    },
    sitemap: 'https://myfrenchlist.fr/sitemap.xml',
  };
}